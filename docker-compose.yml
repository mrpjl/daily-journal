version: '3.8'

services:
  emotion-tracker:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/emotions.db
    restart: unless-stopped
